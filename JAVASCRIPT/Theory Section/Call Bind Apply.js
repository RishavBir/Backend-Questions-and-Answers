



/*

Call >>>>   Call() invokes the function  and allow us to pass in arguments one by one.

Bind() >>>> It returns a new function allowing us to pass in this array and any number of arguments.

Apply() >>>> It invokes the functions and allow us to pass in the argument as an array.

*/


/*

In Short ,
 call() invoke the function immediately,
 bind() returns a new function with the this value set,
 and apply() takes array of arguments instead of list of arguments.

*/